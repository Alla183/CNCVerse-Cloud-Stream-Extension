version = 3

cloudstream {
    language = "uk"
    description = "DoramaLand - Doramas"
    authors = listOf("Alla183")

    status = 1

    tvTypes = listOf(
        "TvSeries",
        "AsianDrama"
    )

    iconUrl = "https://dorama.land/favicon.ico"
}

android {
    buildFeatures {
        buildConfig = true
    }
}
